import { Theme } from "@/constant";
import AccountInfo, { AccountInfoProps } from "./AccountInfo";
import { Action, ActionBar } from "./ActionBar";

interface PropsType {
  className?: string;
  accountInfo: AccountInfoProps;
  showSetting?: boolean;
  onClose?: () => void;
  onBack?: () => void;
  onChangePayPin?: () => void;
  actions?: Action[];
  theme?: Theme;
}
const ConnectedMain = ({ accountInfo, showSetting, onChangePayPin, actions, theme = Theme.LIGHT }: PropsType) => {
  return (
    <div className="uikit-px-5 uikit-pb-[70px]">
      <AccountInfo showSetting={showSetting} accountInfo={accountInfo} onChangePayPin={onChangePayPin} theme={theme} />
      {actions && <ActionBar actions={actions} />}
      {/* <ToolBox /> */}
    </div>
  );
};

export default ConnectedMain;
