import React from "react";

interface ToolBoxItem {
  id: number;
  title: string;
  icon: string;
  onClick: () => void;
}

interface ToolBoxListProps {
  items?: ToolBoxItem[];
}

const ToolBox: React.FC<ToolBoxListProps> = () => {
  return (
    <div className="uikit-flex uikit-py-4 uikit-px-[30px] uikit-h-[105px]" />
  );
};

export default ToolBox;
