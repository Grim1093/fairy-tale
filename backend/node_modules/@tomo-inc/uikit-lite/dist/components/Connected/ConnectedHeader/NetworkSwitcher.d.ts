import { NetworkOption } from '../ConnectedMain/NetworkPopup';
interface PropsType {
    className?: string;
    selectedNetwork: NetworkOption;
    onClick?: () => void;
}
declare const NetworkSwitcher: ({ className, selectedNetwork, onClick, }: PropsType) => import("react/jsx-runtime").JSX.Element;
export default NetworkSwitcher;
